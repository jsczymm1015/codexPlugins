# 插件交付与同步规则

用户指定的插件仓库为 https://github.com/jsczymm1015/codexPlugins 。

- 每次创建或更新插件，在适用测试与插件格式校验通过后，自动将该插件及必要的 marketplace/使用说明提交并推送到此仓库，不重复询问是否上传或上传目的地。
- 本规则取代此前将 Codex 官方云端发布作为默认目的地的偏好。GitHub 上传不代表 Codex 官方云端发布或账号自动同步。
- 保留仓库已有插件与用户修改；先检查远端变化，正常合并，禁止强推或为解决冲突覆盖无关文件。
- 发布目录为 `plugins/<plugin-name>/`，目录入口为 `.agents/plugins/marketplace.json`。新增条目遵循官方 plugin-creator 工具及 schema，保留现有条目。
- 只上传插件必要源文件、说明、必要资源与测试；不上传凭据、原始聊天资料、私人报告、临时产物、缓存或大模型权重。
- 区分结构校验、单元测试和端到端测试。需要特定服务或生成能力而未验证的部分应明确标注，不虚报测试通过。
- 推送后核对远端提交。遇到认证、权限、真实测试失败或无法安全解决的冲突时，报告具体阻碍。
- 该仓库的规则随克隆传播；要在新电脑的所有项目使用此偏好，需同时保存到该电脑的 Codex 全局 AGENTS.md。
